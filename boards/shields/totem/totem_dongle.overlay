#include "totem.dtsi"

/*
 * The dongle is the central; it has no keys. We override the chosen kscan to
 * none and disable kscan0 so the central doesn't try to scan anything itself
 * — peripherals send position events to it over BLE.
 */
&kscan0 {
    status = "disabled";
};
